# if re-doubled:
... (1m) - X - (XX) - P = pen.
... (1M) - X - (XX) - P = pick a suit

# if natrual overcalled:
(1X) - X - (M) - 
    X = constr+, nat 4+M
    2M = cuebid. 3M = Western
    2X/3X = nat
otherwise X = responsive

# if transfer overcalled: basically
(1X) - X - (transfer M) - 
    X = responsive
    1M/2M/3M = nat
    2X/3X = nat
    # for very strong GF hands that wants to cue instead, I think X or P then cue?
otherwise X/cue = responsive emphasizing strength/shape