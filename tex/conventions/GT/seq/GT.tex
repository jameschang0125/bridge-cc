sure fit in 2S - 
2N = ask
   3X = feature in X  # means K or Q here. ignore A as it is always useful.
   3S = min w/o feature. 4S = max w/o feature.  # 3N = CoG or non-serious ?
   4X = spl
3X = HSGT/ST in X  # request void/x/xx/Qx/A(+)/K(+)
3S = 6+S inv, but semi-pre
3N = CoG  # jump to 3N is CoG. otherwise 3M+1 = FF
4C+ = spl

# for 2H: 2S = ask, and exchange the meaning of 2N and S
# if resp promises (usually) unbal (i.e. 1D opener), then after ask: 3X = spl, 4X = void spl