# vs transfers [no matter we have opened or not, even vs transfer responses]
X = t/o and may be lighter than usual  # X then X: slightly stronger than usual
P then X = PEN
P then bid = often lighter
P then 2N = unusual
direct cuebid = Micheals
others = same as original

# vs multi [we are opener]
X = any GF. FP ON.
P then X = t/o-ish
others = if 1N - (multi) then Leb ON, else nat

# vs multi [opp opened]
P then X = t/o
X = 13-15, bal; or t/o then rebid
    subseq Leb ON. X = t/o.
2N = 16-18
others = nat

# vs assumed-fit [we are opener]
X(XX) = inv+. PEN in at least one suit.
    # subseq: resp's X = PEN, P = t/o. open's X = coop PEN.
P then X = inv+. flat (near bal, basically 3+ cards in opp's possible suits)
cuebid = see UvU

# vs assumed-fit (2D/H = some sort of MMs)
X = 13-15, bal; or t/o then rebid
    # subseq: X = coop PEN. 2N = Leb
2H (if available) = nat
2S = minors t/o
2N = 16-18
others = nat  # 3M = 16-18, 6+M