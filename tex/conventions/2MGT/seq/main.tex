\begin{bidsemi}
\bid{fit in M}
\bid{suit < 2M}[still nat and forcing]
\bid{2M+1}[ask feature]
    \subbid{bid}[mid+, feature (KJ+), 2N replaces S if M = H]
        \subsubbid{other bid}[cue]
    \subbid{3M}[min]
    \subbid{3M+1/4M}[max w/o feature (always 3M+1 is better, but 4M is still max)]
    \subbid{jump}[spl, 3N replaces spl S if M = H]
\bid{other < 3M}[HSGT/ST, want void/x/xx/Qx/A(+)/K(+)]
    \subbid{3M}[min]
    \subbid{3M+1/4M}[max]
    \subbid{jump}[spl, 3N replaces spl S if M = H]
    \subbid{other bid}[feature]
\end{bidsemi}