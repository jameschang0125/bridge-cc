# if not forced
P = nothing special
X = t/o
# no matter forced or not
2X = nat
2N = transfer to 3C, then bid/P = comp (or min if forced)
    3C = accepted. min.
    3X = s/o
    3Y (resp's suit) = this is a special case:
        if inv is not possible => s/o  # ex: 1m - (1S) - X ...
        if there are cuebid availble under Y => s/o
        otherwise => GF
    other = GF
bid = (mild) inv. if resp already shows inv, then GF.

# exception: if partner free bids 2M, then showing fit is too urgent so that direct 3M is just min. However, good-bad is still on, just usually won't have M fit.