# if 2M: 6-cards
raise = s/o
2S = F1
2N = ask shortness. usually inv+. may have 6H
    # 2S - 2N:
    3C = spl C or bal
        3D = ask. then 3H = spl C (max will GF later). 3S = min bal. 3N = max bal.
    3D = spl D (then +1 ask min/max). 3H = spl H max. 3S = spl H min.
    # 2H - 2N: due to space, max should not pass 3H if there's no asking bids
    3C = spl C or bal
        3D = bid 3S+ if you are spl or max
        3H = bid 3S+ if you are max
    3D = spl D. 3H = spl S min. 3S = spl S max.
3X = F1  # back to 3M = min. 3H usually only 5 cards unless not GF.
jump to 4M = s/o. other jump = spl.

# if 2M: 5-cards
raise = s/o
suit = constr-inv NF  # s/o if doubled
cue = good raise
2N = ask shortness. usually inv+. may have 6H

# if resp PH
suits = fit-show  # s/o if doubled

# other notes
3X - new suit = GF. please bid 3N if no fit (even no stoppers)
3C - 3D = ask 3M. This allows 3C - 3M = 6-cards.
    3H*= no 3M!
    3S = 3S
    3N = 3H
    4D = 33+M
pre - (bid) - X = PEN