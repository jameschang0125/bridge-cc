2D = pre, some 6+M  # can relax to 5+M if NV 3rd seat
    2M/3M = P/C 
        # after 2S, 3H = min H, others = mid+ H, feature
        # 2D - 2H - 2S - bid = fit S, subseq same as 1S - 2S
    P/3C/4M = s/o
    2N = inv+ ask
        3C = mid
            3D = GF ask, then 3H/S = S/H
            3H = P/C
        !3DH = min, transfer
        3SN = max, H/S
    !3D = some 5+M. GF
        3H = 6+H
            3N = 5S. 3S = 6+S. 4S = to play
        3S = 6+S, 1-H
        3N = 6+S, 2H.
        4C+ = 6+S, 3+H, cue.
    !4C = FP ON. transfer to me.
    !4D = bid yours. may be preemptive

2M = pre, some 5M5m
    2S = 6+S, NF  # very mild encouraging, probably only raise with 2+S max
    2N = inv+ ask
        3C/D = min nat
        3H/S = max C/D
            new suit = nat GF
    3C = P/C
    3D = inv+ fit  # TODO: change 2S - 3D = H ?
    3M = pre
    3oM = nat 6+oM inv NF  # so GF uses 2N first
    4CD = spl
    4HS = s/o

3N = solid 7m, at most 1 outside K
    4C/5C/6C/7C = P/C
    4D = ask control
    4M = s/o

# vs 1N, some adjustments are made (can be relaxed a bit if PH or partner PH)
2D = 10+, 6+M; or 8+ if really good shape
2D - 3C = NF but mild inv
2M = 10-15, 5M4m; 8-15, 5M5m
2M - 3M = constr raise  # roughly inv 14-15. therefore 3D inv