# delay fit
if 2M available: 2M = fit, 3M = almost slam-force. 4M = min.
otherwise: 3M = extra. 4M = min (fast arrival)  # +1 = FF as always
    [after C double fit in 3M] no 2RKC.
    [after D/H double fit in 3M] use 2RKC.
if resp made an unnessesary jump: usually M (oM) fit splinter. these are sure ones:
    1M - 2DH - 2M - jump
    1M - 2C - 2D - jump
    1M - 2X - 2N - jump
    1M - 2X - 2oM - jump  # oM fit

# after raises
3N = min, s/o
4m = mild slam inv
4M = dead min
others = show/ask stopper, but can be cuebid before ST
# cuebid first so that RKC/ERKC is clearer. jump new suit may be treated as auto-splinter

2N = waiting, but usually SI since in this system, we show the important infos (M/oM/fit) first.
rebid or non-reverse 3X = nat SI
!reverse 3X = ask/show stopper; or nat 65 and will promise rebid