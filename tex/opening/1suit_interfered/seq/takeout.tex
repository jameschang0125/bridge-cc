# minors: (1N -> 2C; 2N is fit inv; others unchanged)
XX = inv+. FP ON.
P then X = inv+, flat shape. FP ON.
1X system ON. subseq ON if no interference.
1C - (X) - 1N = transfer to C, constr (about 8-9(10)) C suit. Accept transfer = no extra
1D - (X) - 1N = transfer to C, good constr+ (about 9+) C suit. Accept transfer = no extra
2C = nat weak
2DHS = system ON. (but 1D - 2D excludes inv+ 4+ fit)
3X = system ON
2N = inv 4+ fit  # 5+ for C

# majors: (big changes: transfer from 1N)
XX = inv+. FP ON.
P then X = inv+, flat shape. FP ON.
1S = 4+S nat (then NO Gazzilli, support double ON). 
transfer from 1N, showing a weak hand (5)6+ cards suit and 6+. Accept transfer = no extra. After transfer, back to 2M = 2+M, inv. Others = nat. new suit = GF.  # opener treat this as a weak-two strength
2M-1 = good constr+ (~Drury range i.e. 9+) raise. 2M = (4)5-8 raise
2M+1 above = system ON